<div class="main">
  <div class="wrapper">
    <table>
      <tr>
        <th>Recipes</th>
      </tr>
      <% @recipes.each do |recipe| %>
        <tr>
          <td><a href="/recipes/<%= recipe.id %>" ><%= recipe.name %></a></td>
          <td><img src=<%= recipe.avatar.url %> alt=""></td>
        </tr>
      <% end %>
    </table>
  </div>
</div>
